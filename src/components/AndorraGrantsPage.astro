---
import Container from "@components/container.astro";

const { content } = Astro.props;
---

<Container class="pb-16">
  <section class="mt-14 max-w-4xl">
    <p class="text-sm font-semibold uppercase tracking-[0.16em] text-indigo-700">{content.eyebrow}</p>
    <h1 class="mt-3 text-4xl md:text-5xl font-bold tracking-tight text-slate-900">{content.title}</h1>
    <p class="mt-5 text-lg text-slate-700 leading-relaxed">{content.intro}</p>
  </section>

  <section class="mt-10 rounded-2xl border border-indigo-200 bg-indigo-50/70 p-6 md:p-8 max-w-4xl">
    <h2 class="text-2xl font-semibold text-slate-900">{content.officialSource.title}</h2>
    <p class="mt-3 text-slate-700 leading-relaxed">{content.officialSource.description}</p>
    <a
      href={content.officialSource.url}
      target="_blank"
      rel="noopener noreferrer"
      class="inline-flex mt-5 items-center font-semibold text-indigo-700 hover:text-indigo-900 underline underline-offset-4"
    >
      {content.officialSource.cta}
    </a>
  </section>

  <section class="mt-12 max-w-4xl">
    <h2 class="text-2xl font-semibold text-slate-900">{content.subsidySectionsTitle}</h2>
    <p class="mt-3 text-slate-700 leading-relaxed">{content.subsidySectionsIntro}</p>
    <ul class="mt-5 grid gap-3 md:grid-cols-2">
      {content.subsidySections.map((item) => (
        <li class="rounded-xl border border-slate-200 bg-white px-4 py-3 text-slate-700">{item}</li>
      ))}
    </ul>
  </section>

  <section class="mt-12 max-w-4xl">
    <h2 class="text-2xl font-semibold text-slate-900">{content.keyPointsTitle}</h2>
    <div class="mt-5 space-y-5">
      {content.keyPoints.map((point) => (
        <article class="pl-5 border-l-4 border-slate-300">
          <h3 class="text-xl font-semibold text-slate-900">{point.title}</h3>
          <p class="mt-2 text-slate-700 leading-relaxed">{point.description}</p>
        </article>
      ))}
    </div>
  </section>

  <section class="mt-12 max-w-4xl">
    <h2 class="text-2xl font-semibold text-slate-900">{content.howToApplyTitle}</h2>
    <ol class="mt-5 space-y-4">
      {content.steps.map((step, idx) => (
        <li class="flex gap-4">
          <span class="mt-0.5 inline-flex h-7 w-7 shrink-0 items-center justify-center rounded-full bg-slate-900 text-sm font-semibold text-white">
            {idx + 1}
          </span>
          <p class="text-slate-700 leading-relaxed">{step}</p>
        </li>
      ))}
    </ol>
  </section>

  <section class="mt-12 max-w-4xl rounded-2xl border border-amber-300 bg-amber-50 p-6 md:p-8">
    <h2 class="text-2xl font-semibold text-slate-900">{content.legalNotice.title}</h2>
    <p class="mt-3 text-slate-700 leading-relaxed">{content.legalNotice.body}</p>
    <ul class="mt-5 list-disc pl-6 space-y-2 text-slate-700">
      {content.references.map((reference) => (
        <li>
          <a href={reference.url} target="_blank" rel="noopener noreferrer" class="underline underline-offset-4 hover:text-slate-900">
            {reference.label}
          </a>
        </li>
      ))}
    </ul>
  </section>
</Container>
