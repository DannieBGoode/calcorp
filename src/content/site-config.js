/**
 * Site-wide configuration.
 *
 * Social handles: set to a string (e.g. "@calcorp") to enable the
 * corresponding meta tags. When undefined or null the tag is suppressed.
 */
export const siteConfig = {
  twitterHandle: undefined, // e.g. "@calcorp"
};
